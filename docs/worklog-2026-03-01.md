# Worklog - 2026-03-01

## Summary
- Refocused product direction toward a Neverland staking helper DApp.
- Reworked dashboard and token markets UX from MVP cards into strategy-first and leaderboard-first views.
- Added no-leverage strategy estimator inputs and live calculation flow.
- Added featured pricing backend endpoint with MON/BTC/DUST/shMON support and token icon payloads.

## Product / UX Changes
- Removed Token Prices search and category filters for MVP simplicity.
- Fixed layout issue where footer content was misplaced.
- Rebuilt Token Prices as a market leaderboard table (rank, asset, price, 24h, market cap, volume, source).
- Applied Neverland-inspired visual refactor (palette, typography, atmosphere, components).
- Updated app favicon and sidebar brand icon source to Neverland icon.
- Added Strategy Core section to Dashboard with three expanded strategy cards:
  - No Leverage (0.0x)
  - Leverage 1.5x
  - Leverage 1.8x

## No-Leverage Strategy (Current)
- Added configurable inputs:
  - Input amount
  - Input token selection (MON / USDC)
  - Result unit selection (MON / USDC)
  - Neverland Supply APY (%) manual input (temporary)
- Added live estimate output:
  - Estimated 1Y earnings
  - Live APY display
  - APY breakdown note
- Corrected APY modeling:
  - Previous version used only shMON APY.
  - Current version uses compounded two-layer APY:
    - Combined APY = (1 + shMON APY) * (1 + Neverland supply APY) - 1

## Backend Changes
- Added `/api/price/featured` to return featured assets for dashboard/market usage.
- Added DUST pricing by explicit contract address:
  - `0xad96c3dffcd6374294e2573a7fbba96097cc8d7c`
- Added icon URL fields in featured payload.
- Improved price caching logic (query-keyed cache) and featured response resilience.

## UI Cleanup
- Converted Chinese UI copy/comments in `index.html` to English.
- Refined strategy selector/input controls and removed redundant number spinners.
- Removed redundant plus/minus amount controls after switching to direct amount input.

## Outstanding / Next
- Replace manual Neverland supply APY input with real Neverland API source.
- Validate and lock final APY formula assumptions with product requirements.
- Implement 1.5x and 1.8x strategy calculators based on finalized risk/borrow logic.

